using Terraria.ModLoader;

namespace Modular
{
    class ModularGlobalTile : GlobalTile
    {
        public override void RandomUpdate(int i, int j, int type)
        {
            base.RandomUpdate(i, j, type);
        }
    }
}
